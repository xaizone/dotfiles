#!/bin/sh

dir=~/image/wallpaper

if [ -z "$1" ]; then
  bg="$(find "$dir" -type f -name "*.jpg" -o -name "*.png" | shuf -n1)"
else
  bg="$1"
fi

convert "$bg" ~/.local/share/bg.jpg
feh --no-fehbg --bg-scale ~/.local/share/bg.jpg
