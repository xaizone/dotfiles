#!/bin/sh

while true
do
	cpu="$(grep -o "^[^ ]*" /proc/loadavg )" 
	memory="$(free -h | sed -n "2s/\([^ ]* *\)\{2\}\([^ ]*\).*/\2/p")"
	time="$(date "+[%d-%m-%Y %H:%M]")" 
	xsetroot -name "[MEM: $memory] [CPU: $cpu] $time"
    sleep 5
done 

